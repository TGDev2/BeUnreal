export * from "./dist/index.js";

// ---------------------------------------------------------------------------
//  React-Router v5 compatibility ─ adds back a minimal withRouter HOC
//  Needed by @ionic/react-router until it migrates to v6.
// ---------------------------------------------------------------------------
import React from "react";
import {
    useLocation,
    useNavigate,
    useParams
} from "./dist/index.js";

/** @deprecated shim for libraries that still expect withRouter (RR-v5) */
export function withRouter(Component) {
    function ComponentWithRouterProp(props) {
        const location = useLocation();
        const navigate = useNavigate();
        const params = useParams();

        return (
            <Component
                {...props}
                /* RR-v5 compatible shape */
                history={{
                    push: navigate,
                    replace: (...args) => navigate(...args, { replace: true })
                }}
                location={location}
                match={{ params }}
            />
        );
    }

    const name = Component.displayName || Component.name || "Component";
    ComponentWithRouterProp.displayName = `withRouter(${name})`;
    return ComponentWithRouterProp;
}
